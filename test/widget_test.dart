import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic placeholder test — UI tests require a running backend
    expect(1 + 1, equals(2));
  });
}
